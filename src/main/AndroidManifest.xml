<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.wholegroup.tetroid" android:versionCode="1" android:versionName="1.0" >

	<uses-sdk android:minSdkVersion="4" />
	<uses-permission android:name="android.permission.VIBRATE" />

	<application android:icon="@drawable/icon" android:label="@string/application"
		android:allowBackup="true">
		<activity android:name=".StartActivity" android:theme="@android:style/Theme.Black.NoTitleBar">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>

		<activity android:name=".ScoreActivity" />
		<activity android:name=".PlayActivity" android:theme="@android:style/Theme.Black.NoTitleBar" />
		<activity android:name=".SettingsActivity" />
		<activity android:name=".BonusActivity" />
	</application>
</manifest> 